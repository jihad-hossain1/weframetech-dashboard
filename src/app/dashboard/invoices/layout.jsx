import React from 'react';

const InvoicesLayout = ({children}) => {
    return (
        <div>
            <nav>invoices nav here...</nav>
            {children}
        </div>
    );
};

export default InvoicesLayout;